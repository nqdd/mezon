use gpui::{
    div, prelude::*, px, rgb, size, App, Bounds, Context, Window, WindowBounds, WindowOptions,
};

pub mod root;
pub mod theme;
pub mod title_bar;

pub use root::RootView;
pub use theme::Theme;
