import { size } from '@mezon/mobile-ui';
import { StyleSheet } from 'react-native';

export const style = () =>
	StyleSheet.create({
		channelListUserVoiceWrapper: {
			flexDirection: 'row',
			marginLeft: size.s_30
		}
	});
